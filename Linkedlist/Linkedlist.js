class Node {
    constructor(data) { // Fixed typo here
        this.data = data;
        this.next = null; // Use 'null', not 'Null'
    }
}

class LinkedList {
    constructor() { // Add a constructor method
        this.head = null; // Initialize head inside the constructor
    }
}

class insert_in_start{
    constructor(){
        this.head = null;
    }
    
}